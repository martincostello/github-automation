name: update-dotnet-sdks-for-nightly

on:
  schedule:
    - cron:  '00 10 * * *'
  workflow_dispatch:

permissions: {}

jobs:
  update-from-nightly:
    uses: ./.github/workflows/update-dotnet-sdks.yml
    permissions:
      contents: read
      id-token: write
    secrets: inherit # zizmor: ignore[secrets-inherit] Usage is safe
    with:
      branch: 'dotnet-nightly'
